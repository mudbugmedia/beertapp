<!-- <h1>Listing beer_profiles</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Brewery</th>
      <th>Style</th>
      <th>ABV</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @beer_profiles.each do |beer_profile| %>
      <tr>
        <td><%= beer_profile.name %></td>
        <td><%= beer_profile.brewery %></td>
        <td><%= beer_profile.style %></td>
        <td><%= beer_profile.abv %></td>
        <td><%= link_to 'Show', beer_profile %></td>
        <td><%= link_to 'Edit', edit_beer_profile_path(beer_profile) %></td>
        <td><%= link_to 'Destroy', beer_profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->


<div class="lg3-lg6">
	<div class="hero clearfix">
	<a href="/"><img src="/images/logo.png" alt="Beer Tap"></a>
	<h1>Your Personal Beer Journal</h1>
	</div>

  Search for a new beer profile.

  <%= form_tag search_beers_path, :method => :get do %>
    <%= text_field_tag 'q' %>
    <%= submit_tag "Search for beer" %>
  <% end %>

</div>
