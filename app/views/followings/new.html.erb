<h1>New following</h1>

<% @users.each do |user| %>
  <%= form_tag followings_path do %>
    <%= hidden_field_tag "following[followed_id]", user.id %>
    <%= user.email %>
    <%= submit_tag "Add "%>
  <% end %>
<% end %>
<%= link_to 'Back', followings_path %>
